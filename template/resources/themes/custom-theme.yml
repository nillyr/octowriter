extends: "default"

page:
  margin: [1in, 0.67in, 1in, 0.67in]
  numbering:
    start-at: 3

header:
  border-color: "#DDDDDD"
  border-width: 0.25
  font-size: 8
  height: $base_line_height_length * 5.5
  recto:
    left:
      content: image:logoipsum_auditor.svg[width=125,position=center]
    center:
      content: |
        {filename}{outfilesuffix} +
        {project_manager_full_name} - {project_manager_email}
    right:
      content: image:logoipsum_auditee.svg[width=125,position=center]
  verso:
    left:
      content: $header_recto_left_content
    center:
      content: $header_recto_center_content
    right:
      content: $header_recto_right_content

base:
  font-size: 11
  font-style: "normal"

heading:
  chapter:
    break-before: "always"

caption:
  font-style: "bold"

table:
  head:
    font-style: "bold"
    text-transform: "uppercase"

ulist:
  marker:
    font-size: 11

link:
  font-color: "#C00000"

role:
  # used for conformity
  compliant:
    font-color: "#28591C"
    font-style: "bold"

# used for non-conformity block
example:
  border-color: "#C00000"
  border-width: 1
  font-size: 11

footer:
  font-size: 8
  height: $base_line_height_length * 3.5
  recto:
    left:
      content: ""
    center:
      content: "{confidentiality-level}"
    right:
      content: "{chapter-title} | Page {page-number} / {page-count}"
  verso:
    left:
      content: "Page {page-number} / {page-count} | {chapter-title}"
    center:
      content: $footer_recto_center_content
    right:
      content: ""
